{
  "buildCommand": "yarn build",
  "installCommand": "python3 -m pip install --user setuptools wheel && export SETUPTOOLS_USE_DISTUTILS=stdlib && yarn install --network-timeout 300000",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "memory": 2048,
      "maxDuration": 60
    }
  },
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "s-maxage=60, stale-while-revalidate"
        }
      ]
    }
  ]
}
