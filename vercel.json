{
  "installCommand": "mv backend backend.temp && yarn install --production=false && mv backend.temp backend || (mv backend.temp backend 2>/dev/null; exit 1)",
  "buildCommand": "yarn build",
  "framework": "nextjs",
  "regions": ["iad1"],
  "git": {
    "deploymentEnabled": true
  },
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  },
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "s-maxage=60, stale-while-revalidate"
        }
      ]
    }
  ]
}
